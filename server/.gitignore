# Runtime data: SQLite DB and generated JWT secret — instance-specific,
# contains password hashes and a signing secret, never commit.
data/

# Dart build cache / compiled release binaries.
.dart_tool/
*.exe
